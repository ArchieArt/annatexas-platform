## Shared Analytics Container Architecture — Phase 1 of Analytics/CRM Work Order (BUILT + checkpointed + deployed 2026-07-06)
The founder submitted a huge 8-module Analytics/CRM/Communications Work Order. Agreed working style = build in **approved phases**; Aubre' approved Phase 1 ("yes- perfect"). Phase 1 delivered: (1) an Analytics Audit report, (2) shared New User Feed, (3) shared Business Summary tile, (4) the reusable container architecture the rest of the Work Order builds on.

**ARCHITECTURE PRINCIPLE (foundation for all future phases):** ONE data container per metric in `lib/containers.ts` (single source of truth, real DB rows) → role-filtered at the API layer in `app/api/analytics/containers/route.ts` (role read from SESSION, never client params) → consumed by shared client components in `components/analytics/*` that render identically on ANY dashboard. Edit the container once, every dashboard updates. Future metrics (Revenue, Page Views, Funnel stages, Morning Brief) = add a function to `lib/containers.ts` + a case to the container API + a shared component.

- **`lib/containers.ts`:** `RangeKey='day'|'week'|'month'|'all'`; `rangeStart(range)`; `getNewUsers(range,{includePII,limit})` → `NewUsersContainer{range,total,byRole,rows[]}` (email stripped unless includePII); `getBusinessSummary()` → `BusinessSummaryContainer{totalProfiles,claimedProfiles(CLAIMED|CERTIFIED),phantomProfiles,certifiedProfiles,cardsActivated(cardSlug!=null),paidProfiles(setupFeePaid),claimedRate}`.
- **`app/api/analytics/containers/route.ts`** (force-dynamic): `?container=new-users&range=...` or `?container=business-summary`. ALLOWED_ROLES=['ADMIN','CITY','CHAMBER'] else 403. ADMIN → includePII + canManage:true; CITY/CHAMBER → PII-stripped, canManage:false. Returns `{container,scope:role,canManage,...data}`.
- **`components/analytics/business-summary.tsx`** (shared, useSWR 60s): 4 tiles — Total Profiles, Claimed(+%), Digital Cards Live, Unclaimed (Call Sheet). Prop `showHeading?` default true.
- **`components/analytics/new-user-feed.tsx`** (shared, useSWR 30s): range buttons (Today/Week/Month/All), byRole badges, expandable rows (email[admin]/businessName/onboarding/join date), ADMIN-only Delete w/ inline confirm → `DELETE /api/admin/users?userId=`. Hides delete when `!canManage`. `timeAgo()` helper.
- **`app/api/admin/users/route.ts`** gained a **DELETE** handler (ADMIN-only): cannot delete self or another ADMIN; uses `prisma.$transaction([lead.deleteMany({newcomerId}), user.delete])` because `Lead.newcomerId` is a REQUIRED relation with NO cascade (would FK-block deletion). Logs `[AUDIT]`.
- **Wired in:** admin-dashboard.tsx = new FIRST tab "Daily Brief" (Sparkles icon), `defaultValue="brief"`, renders `<BusinessSummary/>` + `<NewUserFeed/>`. municipality-dashboard.tsx (both City + Chamber variants) = same two components inserted right after header (proves edit-once-update-everywhere; PII stripped, no delete).
- **`ANALYTICS_AUDIT.md`** (deliverable at nextjs_space root): documents TWO analytics streams — Stream A = first-party `AnalyticsEvent` DB table via `lib/track.ts` (dashboard source of truth, privacy-first: role not name), Stream B = GA4 via `lib/analytics.ts` (supplementary, lives in Google console). Findings: 15/19 event types actively emitted (enum-only w/ no emitter: OFFER_REDEMPTION, EVENT_RSVP, VOLUNTEER_SIGNUP); 14/16 funnel stages detectable now; gaps = Stage 16 Viewed Dashboard (needs DASHBOARD_VIEW event), Stage 12 Created Survey (admin-only today), and a LOGIN event for "number of log-ins". (A simple PDF + Word copy were auto-created alongside — do NOT regenerate.)
- **STATUS: build-tested (tsc EXIT 0 + build passed) + checkpointed + deployed to annatexas.org. NOT browser-UI-verified (new feature). Shared dev/prod DB → data live immediately.**

## "The State of Anna" Executive Briefing hero (BUILT + checkpointed + deployed + UI-VERIFIED 2026-07-06)
Trigger: the **Mayor of Anna requested a sample showing of AnnaTexas.org** — the B2G moment. Aubre' gave full creative latitude ("make this thing SING"). Decision: reframe the City/Chamber dashboard so it OPENS like a government-grade economic-development briefing in the language a Mayor speaks, leading with STRONG honest live numbers + one transparent, defensible economic-impact projection. Deliberately AVOIDS weak early-stage ratios (onboarding ~2%, lead conversion 0%) as headline numbers — those still appear lower in the existing "Platform Impact" panel (below the new hero), which is acceptable.
- **`components/analytics/city-executive-brief.tsx`** (NEW, default export `CityExecutiveBrief`, prop `variant:'municipality'|'chamber'`): framer-motion entrance; hydration-safe `useCountUp` (animates after `mounted` flag, requestAnimationFrame cubic ease); date set in useEffect w/ `suppressHydrationWarning`; "Live data" pulse dot. `title-font` "The State of Anna" heading. 4 hero KPI cards (Local Businesses Mapped +certified sub, New Residents Welcomed, Neighbor→Business Connections, Resident Voices Captured). Opportunity band (phantom count = "businesses ready to activate / warm call-sheet"). Economic-impact projection card (badged "PROJECTION", green gradient, shows `$projectedAnnualLocalImpact` + transparent `impactAssumptionLabel` footnote). Narrative strip "What this means for Anna" + 2 trust chips (privacy-first, ready to white-label). useSWR `/api/analytics/containers?container=city-executive` 60s. Dark gradient bg + ambient teal/amber glows. ALL special chars are real UTF-8 (→ · ' — …) NOT \uXXXX escapes.
- **`lib/containers.ts`:** appended `IMPACT_MODEL={avgLocalTransaction:285, annualRepeatVisits:6}` (exported, conservative/defensible), `CityExecutiveSummary` interface, `getCityExecutiveSummary(audience:'city'|'chamber'|'admin')` → real counts + `projectedAnnualLocalImpact = localConnections × 285 × 6` + `impactAssumptionLabel` stating the formula.
- **container API route:** added `case 'city-executive'` (maps session role → audience: CHAMBER→chamber, CITY→city, else admin). Still ALLOWED_ROLES-guarded.
- **Wired as HERO (top of dashboard):** municipality-dashboard.tsx (replaced old plain BarChart3 header block with `<CityExecutiveBrief variant={variant}/>`; BusinessSummary+NewUserFeed still below). admin-dashboard.tsx Daily Brief tab (added `<CityExecutiveBrief variant="municipality"/>` above BusinessSummary+NewUserFeed).
- **VERIFIED LIVE NUMBERS the Mayor sees (2026-07-06, shared DB):** 127 businesses mapped (30 Chamber-Certified live), 97 new residents welcomed, 15 neighbor→business connections, 39 survey responses across 26 questions, $25,650/yr projected local economic activity (badged Projection, formula shown). UI-verified at /dashboard/municipality logged in as city@annatexas.org — renders government-grade, all glyphs correct, no hydration/layout issues.
- **NEXT PHASES (await approval each):** Mailroom CRM (contacts/segments/templates/triggers), sales-funnel engine, Morning Brief w/ human-approval send queue, dual-channel Announcements, demographic/industry + revenue/magazine analytics. Deferred: splitting CHAMBER/CITY into CHAMBER_MANAGER/CITY_COUNCIL/CITY_MAYOR roles (current isolation already works; only if City asks).

## SiloReport → Anna business import (DONE 2026-07-06)
Context: Aubre' asked if businesses loaded into SiloReport.org could auto-populate Anna. Answer given: the two apps are separate DBs and do NOT auto-merge (by design); SiloReport work must happen in ITS OWN conversation. First file she sent (`Uploads/data (1).csv`, 999 rows) was a SiloReport nationwide SCAN list — only 6 verified Anna addresses, rest national chains (Kroger, HEB, Dollar General) + McKinney/Dallas/Plano; correctly REFUSED bulk import (would break the hyper-local promise in front of the Mayor). She then pasted a clean **Anna 75409 registered-business / federal-contractor registry** (~45 businesses, all real 75409 street addresses, owner names, phones, emails; many VOSB/SDVOSB veteran-owned, WOSB women-owned, SBA 8(a) minority-owned — great B2G diversity story).
- **`scripts/import-anna-75409.ts`** (idempotent, dedupes by normalized businessName, skips existing): imported all 45 as **PHANTOM / source ADMIN** unclaimed call-sheet providers (city=Anna, state=TX, zip=75409, isVerified false). DBAs used as display name where present (e.g. MSM-NET Security, LG Motorsports, Market-Good, Landscaping Lawn Services, Zetram Solutions). Certification tags added (veteran_owned/women_owned/minority_owned/sba_8a). Categories mapped to the app's 46-category list.
- **RESULT (live in shared prod DB immediately, NO deploy needed — brief reads live via SWR):** providers 127 → **172**; businessesToActivate (phantom call-sheet) → **138**; certifiedBusinesses still 30. The State of Anna briefing now shows 172 mapped / 138 ready to activate.
- **STANDING PLAN:** When SiloReport is upgraded (in its own conversation) to scrape+store physical addresses and export an Anna-only filtered list, bring that CSV back here for another clean dedupe+import via the same script pattern.

## OPHELIA Client Intelligence Engine — Scan Catalog + Super-App Blueprint (deliverable)
- Aubre' asked for (A) a full description of every SiloReport scan/report and what it tells us, and (B) the blueprint for a one-click "client → app ideas" super-app (input: client website+name+phone+address → run all scans → full-site text harvest → AI dossier → OPHELIA app-idea menu). Immediate use case: her Doctor friend wants to see app options.
- I reverse-engineered SiloReport's full scan schema from her real live export `/home/ubuntu/Uploads/data (1).csv` (1000 records, 936 with scan payloads). Documented 11 scan categories: Identity/Contact, Services/Industry, Revenue-Stream detection, Social presence, Tech/Hosting fingerprint, Domain intelligence, Site health, E-commerce/Store, People/Team, AI "Suggestions" (growth-gap engine), Threat Matrix (security — empty in bulk export, generated on-claim).
- Real distributions captured: top industries (Restaurant 167, Beauty 145, Nonprofit 112, Health/Medical 93...); revenue streams; socials (FB 203, IG 156...); hosting (WordPress 208, Wix 30...); top AI suggestions (889 no email capture, 648 no public phone/email, 414 no online booking).
- Deliverable written to `/home/ubuntu/welcome_to_anna/OPHELIA_CLIENT_INTELLIGENCE_ENGINE.md` (auto PDF+docx generated — do NOT regenerate).
- HONEST WALL restated: SiloReport's actual source code + exact JULIETTE/OPHELIA AI prompts live in the townsquare_app (SiloReport) conversation, not accessible here. Part 1 was reverse-engineered from real data.
- Recommendation to her: build the super-app as its OWN new project (via app management console) sharing SiloReport + AnnaTexas databases; reuse SiloReport scan engine + AnnaTexas audience/demographics engine + token/billing + OPHELIA persona. Offered to write a paste-ready kickoff brief for the new project.

## Command Center "One Brain" super-app vision (master hub)
- Aubre' escalated the super-app vision to a full "command center": one project that unifies ALL her apps via a shared "super database" so she can onboard a client once, pick individual reports/scans to run, and an AI agent assembles everything scraped/found about the business to generate app ideas + a Slidersss pitch (intelligent invoicing, project milestones).
- Key honest distinction I gave her: a shared database unifies DATA, not CODE. Finding redundancy / "code one app has that another lacks" is a portfolio audit *I* perform during the build (that's what the Platform Capabilities Report is for), so the command center REUSES each app's best module instead of rebuilding.
- Contribution map: SiloReport = scan engine + standalone reports (raw intelligence); AnnaTexas = demographics/audience engine + dashboards + business-profile tiles; Slidersss = presentation builder (invoicing, milestones, pitch deck); Command Center (new) = intake + scan/report picker + orchestration agent + app-idea generator.
- Build model: its OWN new project, sharing databases of SiloReport + AnnaTexas + Slidersss (read access), reuse-don't-rebuild. Existing apps stay untouched (Mayor demo protected).
- Deliverable written: `/home/ubuntu/welcome_to_anna/COMMAND_CENTER_KICKOFF_BRIEF.md` (auto PDF+docx — do NOT regenerate). Paste-ready brief to start the new project from app management console.

## PENDING / OPEN ITEMS (not yet built) — AnnaTexas storefront "Grow With Us" panel
- Aubre' wants a services panel inside AnnaTexas.org BUSINESS dashboards with 4 "hire us" offerings: (1) Build a Slider, (2) Hire us to analyze your SILO (clients do NOT run scans themselves — she runs analysis as consultant), (3) Hire us to build a website/app, (4) Hire our marketing agency (ads/promos/leads/visibility). Each = "Request This" button capturing a warm lead.
- STILL NEED FROM HER before building: (a) what S / I / L / O each stand for (SILO = 4 products S/I/L/O, to be rebuilt as a NEW app, NOT by reconfiguring siloreport.org); (b) lead delivery choice — Option A (leads list in Admin dashboard only) vs Option B (list + email notification on each request; I recommended B).
- siloreport.org stays Aubre's private consultant cockpit (she uses it when hired; creates new reports + adds scans). Clients never touch it.
- She has NOT yet answered the storefront questions — she pivoted to the bigger command-center vision. Re-surface when she's ready to build the storefront.

## TownSquareApps — refined 4-phase command-center architecture (updated by Aubre')
- Aubre' named the new master project **TownSquareApps** and refined the command-center vision into a precise 4-phase assembly line:
  1. **Create a Client** — intake screen: website, phone, address, social handles, industry, location(s), owner name, email → save as permanent business profile (the folder everything attaches to).
  2. **Select Scans & Reports → "The Mind"** — every scan/report she owns shown as clickable checkbox tiles; ticked ones run and their results flow into "The Mind" (single source of truth about the business). Feeds: SiloReport scans + standalone reports, AnnaTexas demographics/tiles, + new scans she adds.
  3. **Select Build Tools & Features** — a SECOND library of tiles = the building blocks/commands she's used to build her Abacus apps (intelligent invoicing, project milestones, booking, email capture, profile tiles, token/billing, dashboards, AI persona). Each tile shows name/description/purpose. THIS LIBRARY IS HER MOAT — winning modules feed back in after each build.
  4. **Generate the Build Prompt** — engine writes the full command prompt (checklist: begin build, customize, implement selected tools, personalize from The Mind) that she pastes into a fresh Abacus app to build.
- The loop: The Mind = what's true about the client; Tool Library = what she knows how to build; Phase 4 marries them into a paste-ready prompt.
- Build model unchanged: TownSquareApps is its OWN new project sharing DBs of SiloReport + AnnaTexas + Slidersss (read access), reuse-don't-rebuild. Deliverable `/home/ubuntu/welcome_to_anna/COMMAND_CENTER_KICKOFF_BRIEF.md` UPDATED to this 4-phase flow under the TownSquareApps name (auto PDF+docx — do NOT regenerate).

## SILO framework DEFINED (Aubre's diagnostic doctrine) + 6-step process
- SILO is NOT one product — it's Aubre's 4-lens diagnostic framework every client is examined through (also = the "analyze your SILO" storefront card):
  - **S — Security:** website vulnerability analysis / Threat Matrix / risk posture. (emotion: fear)
  - **I — Intelligence:** what AI/automation exists, readiness, what AI tools exist or are normal in their industry, automations. (emotion: ambition)
  - **L — Leads:** traffic, audience, audience pain-points, new-lead generation, where leads aren't captured, loyalty, demographics, customer journey, products, monetization, competitor monetization analysis, contact, onboarding. (emotion: growth)
  - **O — Operations:** sales funnels, marketing, advertising, revenue, social media, content, branding. (emotion: control)
- OPHELIA's 6-step process (truth before invention): 1) Assess what exists (onboard + run SILO scans + AnnaTexas data + pull assets w/ consent); 2) Identify pain-points/goals/audience/competitors (vs industry + North Texas benchmarks, personas); 3) Gather & organize assets into The Mind; 4) Match to Build Library (fits/gaps); 5) Solve & invent uniqueness — OPHELIA PROPOSES, Aubre' DECIDES (human judgment amplified, not automated); 6) Build overview/phases/prototype + paste-ready Abacus prompt.
- HONESTY RULE baked into engine (I insisted, aligns with her ethos): signals that can't be truthfully scraped (live traffic, real lead counts, competitor internals) must be client-granted OR clearly labeled estimates (e.g. "estimated traffic band: low/med/high"). Never print false precision — protects pitch credibility.
- These definitions + 6-step framework now baked into `COMMAND_CENTER_KICKOFF_BRIEF.md` (auto PDF+docx regenerated — do NOT regenerate manually).
- My stance to her: agreed with the plan, gave 2 honest refinements (the estimate-labeling honesty rule; and framing Step 5 as "OPHELIA proposes, you decide" so it amplifies her instinct not replaces it). This was a strategy/validation turn — NO app code was built.

## CORRECTION (Jul 2026): "Grow With Us" four doors live in TownSquareApps, NOT AnnaTexas
- Aubre' clarified: the "Grow With Us" four-doors storefront (1. Build a Slider, 2. Analyze my SILO, 3. Build a website/app, 4. Hire the marketing agency) IS part of the TownSquareApps project she is building — NOT a panel to be built inside AnnaTexas.org.
- Correct architecture: TownSquareApps has TWO sides of ONE app: (a) PUBLIC storefront = the four "Grow With Us" doors where a business raises its hand / requests a service (lead capture: name, contact, which door); (b) PRIVATE side = OPHELIA's command center 4-phase engine (Create Client → Scans into The Mind → Build Tools Library → Generate Build Prompt). A public request flows straight into a new Client record on the private side. One continuous funnel.
- THEREFORE: do NOT build the storefront inside AnnaTexas.org. AnnaTexas stays the civic showpiece/community platform (pristine for Mayor demo). If Aubre' later wants discovery, AnnaTexas can carry a single outbound LINK to TownSquareApps — nothing more.
- This SUPERSEDES the earlier "PENDING: AnnaTexas storefront Grow With Us panel" note and its open questions (S/I/L/O now defined; lead-delivery choice now moot for AnnaTexas since storefront moved to TownSquareApps). The S/I/L/O framework definitions remain valid and are documented above.
- Gave Aubre' a paste-ready message for TownSquareApps instructing it to plan both public (four doors) + private (OPHELIA command center) sides, building private Phase 1 intake first. This was a clarification/strategy turn — no AnnaTexas code changed.

## "Calling All Neighbors" — two-way matchmaking feature (STRATEGY LOCKED, NOT YET BUILT — Jul 2026)
Pure-strategy thread over many turns (Aubre' repeatedly said "change nothing, build nothing — just analyze"). NO code written. Feature is fully specced/agreed at concept level and awaits her explicit "go" (likely post-Mayor-demo, or as a demo centerpiece if she wants the "belonging number").

**ORIGIN:** Aubre' saw Facebook posts from residents seeking work / a babysitter / a church / senior-volunteer work — all with zero replies, "calls for belonging abandoned." She wants a vetted engine that harnesses this: residents opt into tag-based preferences; when a matching listing is published, BOTH the seeker AND the poster get alerted (Google-Alerts-style). Belongs in AnnaTexas.org (community infrastructure), NOT TownSquareApps.

**FINAL NAME = "Calling All Neighbors"** (Aubre' chose it, "Agreed"). Rejected: "MatchMaker"/"Neighborly Match" (too romantic/dating-adjacent); "Anna Connects" (collides with sister app AnnaConnect.art); "Neighbor Network" (too corporate/cool); "Neighbors for a Cause" (too narrow — only fits charity asks); "Anna Front Line"/"Anna on Call" (military/medical connotations). "Anna First" was parked as a possible MISSION TAGLINE (keep-it-local message), not this feature's name. Internal engine/tables can keep any convenient code name; only user-facing labels must feel neighborly. Every listing = a "Call"; residents "answer a Call."

**KEY AGREED DESIGN DECISIONS:**
- **Double opt-in / consent architecture (her refinement):** matching ONLY fires when a poster deliberately creates a dedicated "Calling All Neighbors" listing seeking opted-in residents; no alerts fire off ordinary posts. Kills the Facebook noise problem, raises intent quality, strengthens the vetted-safe/B2G trust story. Trade-off: risk shifts from "too noisy" to "empty marketplace" — SUCCESS DEPENDS ON SEEDING the first wave of listings (a Chamber/outreach job).
- **Consent-gated mutual reveal:** contact revealed only on mutual two-tap Connect.
- **Same 4 tabs on both profile types (Career / Community / Events / Matches Made)** — drop any "Maker/Finder" naming split.
- **The elegant core:** matches table = notifications = analytics (ONE table). A civic "belonging number" ("X acts of belonging, Y connections this quarter") falls out for free = the B2G pitch centerpiece to the City Manager.
- **REUSE, don't rebuild:** onboarding wizard already generates tags; `/api/matches` + newcomer-dashboard already match residents→providers; surveys system exists. Bolt tags onto existing post forms; seed surveys from onboarding answers.
- **FREE LISTINGS model (Aubre's call, agreed):** posting a "Calling All Neighbors" listing + getting matched = ALWAYS FREE (zero tokens) — "the economy supporting its most valued members: each other." Rationale: (1) removes friction at the exact moment you need marketplace liquidity/seeding; (2) morally true to mission; (3) strongest B2G line ("we charge for ads, but helping neighbors find each other is free — always"). **THE LINE THAT PROTECTS REVENUE: "the listing is free; the megaphone is not."** Posting+matching = free; AMPLIFYING (featuring at top, boosting reach, pinning, paid promotion alongside existing ad products) still costs tokens. Free = the human act of asking for help; paid = extra reach/volume.
- **TWO-DIRECTIONAL "reverse ask" is a first-class use case (proven by her real example):** the same engine runs both ways. Forward: resident/newcomer seeks (work, childcare, church, a club). Reverse: a nonprofit/PTA/city seeks businesses (e.g. Anna Arts Council + Rattan Elementary PTA seeking business owners to donate $300 each for 5th-grade art-mural supplies; Facebook buried it; here every qualifying business gets a fair shot + magazine cover-story recognition as the incentive). INSIGHT: for civic/charitable asks, RECOGNITION/optics is the incentive the way tokens are on the commercial side. Two flavors of one engine: everyday neighbor asks vs. civic/charitable rallies ("Neighbors for a Cause" can live as an INSIDE label for the charitable flavor). **The mural story is Aubre's single strongest live demo example for the Mayor pitch — hold onto it.**

**STATUS: analysis/strategy only. Nothing built or changed. Await explicit greenlight before any code.**

## Work Order #3 — Audit Response (July 2026)

Implemented the external "Anna Bible" audit's fixes across security, trust, and honesty:

**Security / money integrity**
- Stripe webhook (`app/api/stripe/webhook/route.ts`) FULLY rewritten: fails closed (503 if no STRIPE_WEBHOOK_SECRET, 400 on bad/missing signature; removed unsigned JSON.parse fallback). Idempotency via new `WebhookEvent` model (P2002 duplicate → received:true,duplicate:true; rolls back marker on processing error). Logic extracted to `processEvent(event)`. Writes `pricePaidCents` from PRICES.
- Schema (additive only, pushed via `yarn prisma db push --skip-generate` + generate): Provider +`ownershipVerified` +`certifiedConnector` (Bool default false); TokenPurchase +`pricePaidCents Int?` (kept legacy `pricePaid Float`); new `WebhookEvent` model.
- Added `.gitignore` + `.env.example`. **STRIPE_WEBHOOK_SECRET is NOT set in .env — user must add real value from Stripe Dashboard for live payments.** Never fabricate it.

**Trust / verification**
- `lib/verification.ts` (`getVerificationBadges`, VERIFICATION_PROMISE) + `components/verification-badges.tsx` — tiered honest badges (Listed/Claimed/Chamber Member/Ownership Verified/Certified Connector/Sponsored) tied to real fields only. Wired into `provider-detail-content.tsx` and `business-quick-look.tsx`; old inline "Chamber Verified" badge removed. `/api/providers/[id]` select now returns ownershipVerified/certifiedConnector.

**Honesty / copy**
- Sierra public prompt (`lib/sierra.ts` `buildPublicSystemPrompt`): added TRUTH & CORRECTION PROTOCOL + nonpartisan/never-spin rules.
- Privacy (`app/privacy/page.tsx`): real processor disclosure (hosting, storage, Stripe, GA4, AI), lead-sharing, retention, deletion & access rights, independent-platform/piloting "Who we are" banner.
- Terms (`app/terms/page.tsx`): moved out of "draft"; independent-platform/piloting disclosure; honest token/refund (non-refundable once activated, no guaranteed outcome).
- EIN: removed FALSE "required by the City of Anna" claim in `token-shop-content.tsx` → honest in-house vetting note ("not a City requirement"). EIN still collected for business vetting.

**Analytics — Anna 10 activation funnel** (`lib/analytics.ts`, GA4): trackOnboardingStarted / trackOnboardingStep / (trackOnboardingCompleted mirrors anna10_completed) / trackMatchesViewed / trackFirstMatchContact. Instrumented `onboarding-wizard.tsx` (started + per-step) and `newcomer-dashboard.tsx` (matches viewed + first contact, once each). DB-backed funnel card deliberately NOT built (would need AnalyticsType enum migration + new dashboard card) — GA4 is the funnel home.

**Empty-state discipline**: already satisfied by existing shared `components/empty-state.tsx` used on stories/survey/offers/kind-deeds/billboard with honest warm copy. No change needed.

**Deliverable doc**: `/home/ubuntu/welcome_to_anna/ANNA_TRUTH_LEDGER.md` (+auto .docx/.pdf) — feature × 8-stage reality ladder (Concept→Specified→Coded→Build Passing→Deployed→Flow Verified→Used by Real Resident→Revenue Producing). Honest: most features at Deployed; stages 6–8 marked NOT yet earned; revenue-producing = not yet.

**Known pre-existing (NOT this work order):** automated QA flags site-wide false positives (header "Community" dropdown trigger, filter tabs, role selectors) + a real pre-existing contrast bug on `/neighbors` (#e7ebef text on #fff). Out of scope; flag to user if revisited.

**Items only the USER can supply** (never fabricate): STRIPE_WEBHOOK_SECRET + key rotation; recorded prod flow walkthroughs; real paid Connector/Stripe evidence; partnership truth sheet; voice-of-customer (5 newcomers/5 businesses); legal review of Privacy/Terms before money flows.

## Work Order #4 — Guest Pass, Contact Form, Footer (deployed to annatexas.org + annatexas.abacusai.app)

**Guest role:** Added `GUEST` to `UserRole` enum (additive, pushed with `--skip-generate`). Guest signup goes through the normal `/api/signup` route with `role: 'GUEST'` — it also accepts `marketingOptIn`, `agreedToTerms` (required for guests, 400 if false), and `interests[]`. Guest extras are stored in `User.preferences` JSON as `{ accessType: 'guest', marketingOptIn, interestPages: [...] }`; `dataConsent` set true for guests. No new columns.

**Guest door:** `/welcome/guest` (static route beside the dynamic `/welcome/[door]`) renders `components/guest-signup-form.tsx`. Fields: name, email, phone (optional), password, interest-page multi-select (all preselected), marketing opt-in (default on), required Terms+Privacy checkbox. On success → signIn → redirect `/guest-hub`.

**Guest hub:** `/guest-hub` (force-dynamic server page) reads session + `preferences.interestPages`, shows chosen pages as cards + remaining pages as pills + a prominent "Request a Newcomer Account" CTA → `/contact?topic=newcomer`. Guest-accessible pages catalog lives in `lib/guest-pages.ts` (GUEST_PAGES).

**Guest gating (middleware.ts):** GUESTs are redirected to `/guest-hub` from `/dashboard`, `/onboarding`, `/settings`, `/neighbors`, `/city-services`. IMPORTANT: `/neighbors` and `/city-services` are PUBLIC pages — the `authorized` callback returns `true` for them (so anonymous visitors are NOT forced to log in); the guest redirect happens inside the middleware function which only runs when authorized passes. Header (`components/header.tsx`) hides "Meet Your Neighbors" and "City Services" nav (desktop + mobile) when `role === 'GUEST'`, and `getDashboardLink()` returns `/guest-hub` for guests.

**Contact Us:** New `/contact` page + `components/contact-form.tsx` + `/api/contact` route. Reuses existing `ContactSubmission` model (name, email, subject, message, formType, status). Topic types: newcomer, business, influencer, town, general — prefilled via `?topic=` query. Sends admin email via `NOTIF_ID_CONTACT_REQUEST` (registered this WO) to hello@annaartscouncil.org, modeled on `app/api/town-request/route.ts`, with `sender_email: noreply@<hostname>`.

**Footer "Get Started" column** now: Join as a Resident, Join as a Guest (`/welcome/guest`), Join our Influencer Program (`/contact?topic=influencer`), List Your Business, Claim Your Listing, Bring It To Your Town, Contact Us.

**Strategic guidance given (deferred, not built):** (1) White-label "Welcome to Melissa / Van Alstyne" factory — recommended introducing a `town.config.ts` seam + fork-and-configure playbook rather than premature multi-tenancy; advised NOT to hand-build town #2 until Anna proves traction. (2) Magazine contributor commission program — infrastructure partially exists in Sierra's TREASURER mode (`creator_pool = 0.30 × ad_revenue`, W-9 gate, admin-approved payouts in `lib/sierra.ts`); missing = contributor signup/agreement flow + role. Flagged worker-classification/tax risk of "exclusive" + commission; recommended "first-right" contractor terms. Both queued as scoped follow-up phases pending user decision.

## Anna LIFE Certified Contributor Program — authoritative spec (uploaded July 2026)

Governing document: `/home/ubuntu/Uploads/Anna_LIFE_Certified_Contributor_Jotform_Blueprint.md` (v1.0, owner Aubre' Murphy). This supersedes the lightweight contributor form shipped in WO#5 and is the source of truth for the contributor program going forward.

Locked requirements (do not change without Aubre's approval):
- Applicants must be 18+ AND currently live in Anna, TX (hard eligibility gates).
- Anna LIFE = uplifting, prestige, dignity-centered publication. Uplifting-FIRST / non-sensational / solutions-oriented (NOT an absolute ban on hard facts — verified corrections, urgent safety info, material conflicts of interest are allowed, handled calmly).
- Editorial certification and advertising commission are SEPARATE agreements (no pay-to-play). Advertising Revenue Partner routes to its own application.
- 10 editorial certification tracks (Community Feature Writer, Interviewer/Oral Historian, Business & Econ Dev Correspondent, Arts & Culture Correspondent, Nonprofit/Kind Deeds Correspondent, Events/Community Life Correspondent, Newcomer/New Beginnings Correspondent, Photographer, Visual Artist/Illustrator, Columnist). Under-18 = future youth program (interest list only).
- AI writing companion available anywhere substantial writing is requested; human remains responsible for facts. Automated tag suggestions, human editor approves.
- Certification is selective; acceptance ≠ guaranteed publication. Final approval belongs to an accountable human editor (Aubre'/Editor-in-Chief).
- Full structure includes: multi-step "card" application (eligibility gate → identity → track/beats → mission & story-judgment exercises → track-specific portfolio → 18-clause Editorial Charter → AI responsibility → conflicts/rights → e-signature → first pitch), internal 100-pt review scorecard, Jotform-style workflow, certification ladder (Applicant→Candidate→Provisional→Certified→Senior→Contributing Editor), orientation + 10-Q quiz (80% pass), certificates, permanent departments, email templates.

Phasing plan agreed with user: build in phases, not all at once. Phase 1 = the public multi-step Certified Contributor application (gates, tracks, charter, AI/conflicts/e-sign, first pitch) + schema fields + admin review. Later phases = internal scorecard, workflow states/certification ladder, orientation quiz + certificates, public contributor profiles/portfolio/assignment dashboard.

## Anna LIFE Digital PRESS Card — build status (WO#6, July 11, 2026)

Built NATIVELY in-app (no external form embed) as a Phase 1–2 slice, reusing the existing digital business-card engine.

**Coded / deployed (both annatexas.org + annatexas.abacusai.app):**
- Schema (additive, no data loss): `PressCredential` (1:1 with an APPROVED `MagazineContributor`) + `PressCredentialAudit` (append-only lifecycle trail) + enums `PressLevel` (PROVISIONAL/CERTIFIED/SENIOR/CONTRIBUTING_EDITOR) and `PressStatus` (ACTIVE/SUSPENDED/EXPIRED/REVOKED).
- `lib/press.ts` — track labels, per-level visual treatment (gold for Certified+, silver Provisional), status treatment + exact public notices, CREDENTIAL_NOTICE disclaimer, slug/credentialId/expiry helpers.
- Public page `/anna-life/press/[slug]` (server component; high-contrast dark-on-light QR with gold only on frame/seal; lit/greyed opt-in action row; Request-This-Contributor button; "no published work yet" placeholder; disclaimer). Client actions in `components/anna-life/press-card-actions.tsx`.
- Public APIs: `GET /api/anna-life/press/[slug]` (safe JSON), `POST .../scan` (privacy aggregate counter), `GET .../vcard` (opt-in fields only).
- Admin API `POST/GET /api/admin/anna-life/credentials` (ADMIN-gated; actions ISSUE/RENEW/SUSPEND/RESTORE/EXPIRE/REVOKE/UPDATE; audit row every change; unique slug deduped across PressCredential.slug + User.cardSlug + Provider.cardSlug).
- Admin issuance controls embedded in `components/admin-magazine-contributors.tsx` (Issue card at level, set level, renew, suspend/restore, revoke, view public card) for APPROVED non-partner contributors.
- Added `coverage` topic to `components/contact-form.tsx` (Request This Contributor routes to `/contact?topic=coverage&contributor=<slug>` with prefilled message).

**Deferred / specified but NOT built (do not claim built):** contributor self-service edit dashboard; full Request-This-Contributor case workflow (records/pipeline/assignment/notifications — today routes to editorial contact desk); article credits + byline portfolio + "Most Read/Most Viewed" rankings (no Article model exists yet); wallet passes / printable badge / scan geo-timeline analytics.

**Safeguards honored:** QR high-contrast (gold on frame/seal only), public contact strictly opt-in, never show address/proof-of-residency/legal name/internal notes/score, contributor cannot self-assign level/dates/ID/status/residency/counts, rankings will read "Most Read/Most Viewed" not "#1 Journalist".

## Anna LIFE PRESS Card — Phase 3 (built & deployed July 11, 2026)

Phase 3 added on top of the Phase 1–2 credential foundation:

- **Contributor self-edit dashboard** — `/anna-life/press/manage` (page.tsx + `components/anna-life/press-manage-form.tsx`). Contributor signs in with the email the editor approved them under; matched via `MagazineContributor.email` → `pressCredential`. They PROPOSE edits to public display fields only (bio, beats, opt-in phone/email, website, social, headshot URL). Stored in `PressCredential.pending*` fields + `hasPendingEdits`/`pendingSubmittedAt`. Live card never changes until an editor approves. No CONTRIBUTOR UserRole — email-match gate by design. Manage API: `app/api/anna-life/press/manage/route.ts` (GET own cred + POST submit-for-review, writes an audit row).
- **Editor review** — pending-edit block in `components/admin-magazine-contributors.tsx` (current-vs-proposed diff + Approve/Discard). New actions `APPROVE_EDITS` (copies pending→public, clears queue) / `REJECT_EDITS` in `app/api/admin/anna-life/credentials/route.ts`.
- **Coverage-request case workflow** — public card button opens in-card modal (`components/anna-life/press-card-actions.tsx`) → POST `app/api/anna-life/press/[slug]/request/route.ts` creates a `CoverageRequest` (status pipeline enum NEW/SCREENING/FORWARDED/ACCEPTED/DECLINED/COMPLETED) and emails editorial desk (NOTIF_ID_CONTRIBUTOR_COVERAGE_REQUEST → hello@annaartscouncil.org). Admin **Coverage** tab (`components/admin-coverage-requests.tsx`) manages cases via `app/api/admin/anna-life/coverage-requests/route.ts` (GET/PATCH). Contributor never auto-contacted — desk screens first.

Still deferred (do not claim): article credits/byline portfolio/rankings (no Article model), wallet passes, printable badge export, QR geo/timeline analytics.

## 2026-07-11 — Provider dashboard build-out (Kimberly / Anna TX Realty)
Three features added to the provider dashboard (code written, tsc+build PASS, checkpoint saved; NOT yet deployed, NOT browser-verified):
1. **Magazine Studio** (`/dashboard/provider/magazine`): providers self-serve purchase an OPEN ad plot in a sellable Anna LIFE issue, upload PNG/JPEG artwork (presign→PUT), pay tokens = ceil(priceCents/100) [1 token = $1]. Plot → RESERVED (editor reviews before print; admin flips to SOLD via existing admin `updateAdPlot`). APIs: `POST /api/magazine/ads/purchase` (atomic $transaction; throws PLOT_TAKEN / INSUFFICIENT_TOKENS), `GET /api/magazine/ads/mine`. Also a "Contribute an article or photo" section linking to `/magazine` + `/gallery`. Ad spend now shows in `/api/providers/[id]/token-history`.
2. **Community Pulse** (`components/provider/community-pulse.tsx`, `GET /api/providers/community-pulse`): growth tiles (New Neighbor Profiles, Businesses Claimed — total + this month) + "Newcomers to Greet" list (ungreeted approved welcome-board intros, last 30d) with "👋 Say hi" → `/welcome-board#post-<id>`.
3. **Ambassador immediate alert**: `lib/welcome-sla.ts` `notifyAmbassadorsOfNewIntro(post)` creates in-app Notification (type `intro_new`) for all ambassadors+admins on each new intro; wired into `/api/welcome-board` POST. In-app only; SLA engine still does 12h/20h email escalation. Kimberly set `isAmbassador=true` (also in seed).
4. **Try Our Apps for Realtors** (`components/provider/realtor-apps.tsx`) on dashboard: external cross-promo links — SILO Report (siloreport.org), Slidersss (slidersss.org), AnnaConnect (annaconnect.art), new tab.

## Anna Opportunity Center (EDC arm) — first slice shipped

The first "civic-leader arm" of the platform. Built on the SHARED data engine (not a silo) so later arms (Mayor, City Manager, Council, Artists) reuse it.

**Role/access:** New `EDC` role in `UserRole` enum. Front door at `/opportunity-center` (public/partner-facing name), gated in `middleware.ts` to EDC+ADMIN; `/dashboard` root redirects EDC → `/opportunity-center`; `components/header.tsx` getDashboardLink maps EDC. Demo login seeded: `edc@annatexas.org` (role EDC, shared demo password `AnnaDemo2026!`, same as chamber@/city@).

**Data model (schema.prisma, appended after CoverageRequest):** `BusinessAssistanceCase` (spine) + `CaseNote`, `CaseReferral`, `CaseAuditLog`. Enums: `AssistanceNeedType`, `CasePriority`, `CaseStatus`, `CaseOutcomeType`. Relations added to User (`submittedCases`/`assignedCases`) and Provider (`assistanceCases`). Consent gate: `consentToEDC`/`consentAt` — a case is ONLY visible to the EDC queue when the business ticks the share box.

**The loop (working end-to-end):** provider submits "What would help your business in the next 90 days?" (component `components/provider/business-assistance-intake.tsx`, in provider-dashboard) → consent-gated → EDC sees it in Priority Queue (`components/opportunity-center/opportunity-center-home.tsx`) → open case dialog (`case-detail-dialog.tsx`): assign to me, change status/priority, add notes, make referral (Chamber/City/ArtsCouncil/Mentor/Other), record outcome → rolls up into Monthly Brief (`monthly-brief.tsx`, separates verified outcomes from still-open, no fake numbers).

**API routes:** `/api/opportunity-center/{intake, cases, cases/[id], cases/[id]/notes, cases/[id]/referrals, summary, brief}`. All EDC-facing routes guarded EDC+ADMIN (401); intake guarded PROVIDER+ADMIN. Shared helpers/labels in `lib/opportunity-center.ts`.

**Deliberately deferred to LATER slices (placeholders shown, no fake data):** Demand Intelligence, Founder Pipeline, Recruitment Pipeline. City PDFs (Comp Plan, Downtown Master Plan) reserved for a later EDC Sierra-advisor grounding slice.

**Note:** Pre-existing app-wide test-tool warnings (inactive filter/tab buttons on welcome-board, news, billboard, signup, offers etc.; low-contrast text on /offers) are NOT part of this arm and were left untouched.

## Gotcha: global body text color is near-white (dark-first theme)
In `app/globals.css`, BOTH `:root` (`--foreground: 210 20% 92%`) and `.dark` (`0 0% 98%`) define a near-white body text color — the whole app is dark-first. Any page/section that forces a LIGHT background (e.g. the cream `#FFF8E8` used on ANNA LIFE / Writer's Desk) MUST set an explicit dark base text color on its wrapper (e.g. `color: '#172A3A'`), otherwise any text element that doesn't hardcode its own color inherits near-white and becomes invisible ("white on light font"). Fixed on `/dashboard/writer` (app/dashboard/writer/page.tsx wrapper) 2026-07-21. Watch for this on every light-background surface.

## Anna-ONLY front-door gate + Business Approvals queue (built & deployed Aug 17, 2026)

Owner rule (Aubre'): only people who LIVE in Anna (city) or the Anna ETJ get full accounts. Melissa / Weston / Van Alstyne / "moving to Anna soon" / anywhere else = GUEST PASS only. Eligible BUSINESSES do not auto-go-live — they APPLY and land in an admin Approvals queue for Aubre' to review before their profile is public.

Files:
- `lib/anna-eligibility.ts` (NEW) — single source of truth. `ANNA_LOCATION_OPTIONS` (anna_city, anna_etj = eligible; moving_to_anna, melissa, weston, van_alstyne, other = not eligible), each with label + businessLabel. Helpers `isAnnaEligible()`, `locationLabel()`, `isKnownLocation()`.
- `prisma/schema.prisma` — added `homeLocation String?` to User; added model `AnnaProfileApplication` (userId, providerId?, businessName, category?, homeLocation, contact*, note?, status PENDING|APPROVED|DECLINED, reviewedBy*, decisionNote?, timestamps; indexes [status,createdAt] and [userId]). db push + generate done.
- `components/signup-form.tsx` — required location `<select>` (first field, business vs resident label), guest-path notice + Terms checkbox when non-eligible, submit label adapts (Get My Guest Pass / Apply for Anna Profile / Create Account). Guest path forces role GUEST, routes to /guest-hub.
- `app/api/signup/route.ts` — server-authoritative gate: computes annaEligible; forces requestedRole='GUEST' when not eligible; stores homeLocation; guests still require agreedToTerms. NOTE: a signup with NO location is treated as not-eligible → forced GUEST (this is why the generic test-tool signup smoke, which sends no location + no terms, returns 400 — not a real bug; real form always sends both). Verified via curl: anna_city newcomer → NEWCOMER 201; melissa resident/provider → GUEST 201.
- `app/api/providers/register/route.ts` — returns 403 {guestOnly:true} for non-Anna businesses; persists location; creates a PENDING AnnaProfileApplication (best-effort) after provider create; provider stays isVerified:false (hidden from public directory) until approved.
- `app/api/admin/anna-applications/route.ts` (NEW) — admin-gated (role ADMIN || isAdminEmail). GET lists applications (default PENDING) + attaches linked provider (businessName/category/city/isVerified/status) + pendingCount. PATCH approve/decline: on approve sets application APPROVED + provider {isVerified:true, status:'CLAIMED'} (directory-eligible); decline sets DECLINED, provider stays hidden.
- `components/admin/anna-profile-applications.tsx` (NEW) — brand-styled panel, listed in admin dashboard APPROVALS tab (ABOVE AdminRoleRequests) in `components/admin-dashboard.tsx`.

Town-duplication decision: advised Aubre' NOT to hand-build MelissaTowneLIFE / VanAlstyneTowneLIFE yet. The Guest Pass IS the demand-capture mechanism (every out-of-Anna signup = logged proof of demand / B2G traction). White-label engine (lib/town-config.ts + TownDoor with Melissa & Van Alstyne seeded as draft doors) already exists — flip a town's door live only once demand crosses a threshold. Awaiting her confirmation before building any second town.

## 2026-08-17 — Admin lifecycle email dropdown + in-app messages (built & deployed both hostnames)

In the admin dashboard User Management/User Activity roster (`components/admin-user-management.tsx`), each user row now has a gold-bordered "✉ Send email…" dropdown alongside the existing role select, Activity/Make Ambassador/Make Official buttons. Options map to the 4 built TowneLIFE lifecycle emails (`lib/emails/townelife-sequences.ts`):
- APPLY → sendBusinessApplicationReceivedEmail (no role change)
- JOHN DOE → sendWelcomeEmail (generic get-started, no role change)
- GUEST → sendGuestApprovedEmail + sets role=GUEST (confirm() prompt)
- REJECT → sendApplicationDeclinedGuestEmail + sets role=GUEST (confirm() prompt)

New endpoint `app/api/admin/users/lifecycle/route.ts` (POST {userId, action}; ADMIN||isAdminEmail gated) sends the mapped email, does the GUEST role move for GUEST/REJECT, AND drops a matching in-app Notification (via `lib/notifications.ts` `notify()`, linkUrl `/my-anna`). Added `GUEST` to the role `<select>` and to `validRoles` in `app/api/admin/users/route.ts` PATCH.

In-app messages on Make Official / Make Ambassador: `app/api/admin/users/route.ts` PATCH now calls `notify()` when isVerifiedOfficial→true (title "You've been made Official ✅", link /my-anna) and when isAmbassador→true (title "You're now a Welcome Board Ambassador", link /welcome-board). Both messages tell the member this is their My Anna in-app inbox.

In-app messages surface in the `/my-anna` UnifiedInbox (reads `/api/roles/inbox`, which pulls Notification rows). NotificationBell component exists but is NOT rendered anywhere — My Anna is the inbox.

Newcomer-welcome-post alert: ALREADY existed and untouched — `lib/welcome-sla.ts notifyAmbassadorsOfNewIntro()` creates `intro_new` Notifications for all ambassadors+admins, wired into `/api/welcome-board` POST.

## 2026-08-17 (correction) — Lifecycle dropdown remapped per Aubre'

Corrected the "✉ Action…" dropdown in `components/admin-user-management.tsx` + `app/api/admin/users/lifecycle/route.ts`:
- JOHN DOE = NO email; archives the account (accountStatus='archived') so it drops out of the User Activity roster. GET filter in `app/api/admin/users/route.ts` now excludes `notIn: ['TEST','archived']`.
- APPLY = sends the business-EXCEPTION invitation email (`sendApplicationDeclinedGuestEmail`, the one with the "APPLY FOR AN ANNA TOWNELIFE BUSINESS EXCEPTION" button). Keeps role GUEST.
- REJECT = used AFTER an exception request is turned down; sends NEW email `sendExceptionNotApprovedEmail` (Email #5 in `lib/emails/townelife-sequences.ts`, subject "Your Anna TowneLIFE Business Exception Request", NOTIF_ID_BUSINESS_EXCEPTION_NOT_APPROVED=62a44afb7). Keeps role GUEST.
- GUEST = `sendGuestApprovedEmail`. Keeps role GUEST.
APPLY/GUEST/REJECT set role GUEST only if not already GUEST; none promote to Provider (Aubre' promotes Guest→Provider manually via role selector). Each of APPLY/GUEST/REJECT also drops a matching in-app Notification (linkUrl /my-anna). JOHN DOE drops none.
Build passed, checkpoint saved, deployed to both hostnames.

## 2026-08-19 — Anna Workforce manual job board (Control Room) — built, checkpointed, NOT yet deployed

Owner (Aubre') can now add jobs to the public Jobs page (`/help-wanted`) manually from the admin Control Room, no business/provider account needed.

- **UI:** `components/admin/anna-workforce.tsx` (default export `AnnaWorkforce`), rendered as a new container at the bottom of `components/admin/control-room.tsx` (imported at top; `<AnnaWorkforce />` after the Token Economy CARD). Shows in admin dashboard `CONTROL_ROOM` tab. Form fields: Position (title, required), Employer/Company (defaults "Anna TowneLIFE"), Type pills (Full-Time/Part-Time/Remote/Hybrid), Pay starts at $ (salary text), Location city (defaults "Anna, TX"), Job link (applyUrl), Tags (comma), Description, and a "Wishlist / alert job seekers" toggle (default ON). Lists existing house jobs with delete.
- **API:** `app/api/admin/jobs/route.ts` (`force-dynamic`, admin-gated via getServerSession + role==='ADMIN' || isAdminEmail). GET lists house jobs (`postedById:null`). POST creates Job with category='Anna Workforce', postedById=null, status='active', isVerified:true, tags[] (max 12); if alertSeekers, calls `notify()` (type 'job_alert', linkUrl '/help-wanted', dedupeKey job_alert:<jobId>:<userId>) for every user with `UserOnboardingSurvey.isJobSeeker=true`, returns {success, job, alerted}. DELETE (?id=) only removes jobs where postedById===null.
- **Schema:** `model Job` — made `postedById` + `postedBy` relation OPTIONAL (nullable) so admin/house jobs need no provider; added `tags String[] @default([])`. `yarn prisma db push` + `generate` done. All existing `postedBy` reads are null-safe.
- **Public page:** `components/help-wanted-content.tsx` — SampleJob gained `tags?: string[]`, apiJobs maps `tags`, and the card renders up to 6 tag chips. NOTE its JobType union is Full-Time/Part-Time/Remote/**Contract** (no Hybrid tab) — Hybrid jobs still show under "All", just no dedicated filter tab. `/api/jobs` GET has no select so it already returns tags+applyUrl.

State: build passed, checkpoint saved. NOT deployed (Aubre' deploys herself).

## 2026-08-19 — Jobs board hardening (rename + share + footer + approval + membership gate)

Follow-up to the Anna Workforce build, all in one checkpoint (build PASS, NOT deployed — Aubre' deploys herself):
- **Rename:** public Jobs page H1 now "Jobs in Anna" (was "Help Wanted in Anna"); route stays `/help-wanted`. Page `<metadata>` title/OG updated to "Jobs in Anna". Filter tabs still Full-Time/Part-Time/Remote/Contract (unchanged).
- **Share:** added `ShareBoard()` inner component in `components/help-wanted-content.tsx` (navigator.share w/ clipboard fallback, Share2/Check icons) in the page header next to the post button.
- **Footer:** added `{ label: 'Jobs in Anna', href: '/help-wanted' }` to the DISCOVER column in `components/footer.tsx` (after Events This Week).
- **Admin approval for EMPLOYER jobs:** `app/api/jobs/route.ts` POST now creates employer jobs with `status:'pending'` (was 'active') and NO token deduction at creation. Public GET already filters `status:'active'` so pending/rejected never show. Moderation via new `PATCH /api/admin/jobs?id=&action=approve|reject`: approve → checks provider has ≥50 tokens, deducts 50, sets status active+isVerified, notifies business (type job_approved) + wishlist-alerts job seekers; reject → status 'rejected', notifies business (job_rejected), no charge. Admin GET now returns `{ jobs (house), pending (employer awaiting review) }`. The Anna Workforce Control Room component shows an "Awaiting review" queue with Approve/Reject buttons.
- **Paying-membership gate:** `app/api/jobs/route.ts` POST requires an active `PlatformSubscription` for the provider's user (status active|canceling and currentPeriodEnd in future) BEFORE allowing a post — returns 403 `needsMembership:true` otherwise. (House jobs created by admin via `/api/admin/jobs` are exempt — still instant/active/no tokens.)
- Copy fixes on the page: employer CTA no longer says "Live instantly" (now "Reviewed by our team, then live"); empty-state no longer says "Post a Job — Free".

## 2026-08-19 — Newsroom placeholder guard + "Anna Co-op Team" byline unification (built + checkpointed, NOT deployed)

Fixes Aubre's complaint that live desk stories showed raw `[insert local business]` / `[NEEDS: ...]` fill-in-the-blanks Sierra never filled, and that named generational personas were being attacked on social media as "AI reporters."

- **Placeholder guard** in `lib/magazine/issue-live.ts`: new exported `hasUnfilledPlaceholder()` + `PLACEHOLDER_RE` (matches `[NEEDS`/`[INSERT`/`[TODO`/`[TK`/`[TBD`/`[FILL`/`[NAME`/`[BUSINESS`/`[DATE`/`[ADDRESS`/`[PRICE`/`[PLACEHOLDER`/`xx+`, and `{{...}}`). `normalize()` returns null if any placeholder is in headline/standfirst/bodyPreview OR any rendered paragraph → held back, never fabricated. `toParagraphs()` now strips ALL leading markers (`[SANDBOX]`, `[TEST DRAFT …]`).
- **GROUND TRUTH (verified against live DB 2026-08-19):** 25 SUBMITTED desk drafts for tenant `anna-tx`. 7 are ~100-word stubs (<400w MIN_WORDS, never shown). Of the 18 full-length (400-782w), **17 carry genuine `[NEEDS: …]` blanks → held**, only **1 is clean & live**: "The Mixtape Generation Buys Its First Anna Home". So the "Fresh from the Newsroom Desk · live" section now shows just 1 story until the blanks are filled with real data. This is honest, not a bug.
- **Byline unified to "Anna Co-op Team"** everywhere: `issue-live.ts` sets `author:'Anna Co-op Team'`; all 18 published stories in `lib/magazine/issue-articles.ts` had persona author names (Marion James/Yvonne Turner/Charles Baker/Angela Smith/Emily Diaz/Jenny Taylor) replaced with 'Anna Co-op Team' (cover stays Aubre' Murphy). `app/anna-life/newsroom/page.tsx`: live section collapsed from per-persona groups to ONE "Anna Co-op Team" group; "Six lenses" roster headers now lead with the generation lens + "Anna Co-op Team" byline (persona names removed); hero + metadata copy de-personalized. `[slug]/page.tsx` "About this byline" footnote reworded for team framing. NOTE: published-story URL slugs still contain old persona names (e.g. marion-property-tax) — stable identifiers, not visible bylines, left untouched.
- Aubre's forward directive (same turn): the blanks should be filled from REAL platform data we already have — claimed business profiles, nonprofit missions, artist products, events, jobs, kind-deed nominations, community photos, Sierra Tuning sync sources, admin analytics — "we have the data but haven't harnessed it to create meaningful content." NEXT build (not yet started): a data-grounded story/brief generator that pulls these real records instead of leaving fill-in-the-blanks. Confirm scope with her before building.

## 2026-08-19 — Unified teal DashboardTopBar across all role cards (built + checkpointed, NOT deployed)

Aubre' said the resident dashboard was "boring"; redesigned it and applied the same treatment to every role card. Shared component: `components/dashboard/dashboard-topbar.tsx` (default export `DashboardTopBar`).
- Renders the pretty-teal welcome banner (linear-gradient #0F5C5B→#14807E→#0BA89F + radial highlight) with "Welcome, {firstName}!" + subtitle + Settings link + optional `rightSlot`, then a horizontal scrollable pill tab bar. Glowing amber (#F2A93B) ping dot on a tab when its count exceeds what this browser last saw (localStorage `annaDash:lastSeen:v1`); inbox dot from real unread count. Dots driven by ONE cheap endpoint `app/api/user/dashboard-status/route.ts` returning `{unreadInbox, offers(=CarePackageClip unused), surveys(active), events}`. No schema changes, all tabs point to EXISTING pages (cost-conscious per Aubre').
- Exported tab sets: `RESIDENT_TABS` (Dashboard/My Offers/My Card/Survey Says/What2Do/Inbox/Matched), `PROVIDER_TABS` (Dashboard→/dashboard/provider, My Card→/card/manage, Grow→/grow, Survey Says, Inbox), `GUEST_TABS` (Explore→/guest-hub, Survey Says, What2Do, Inbox, Become a Newcomer→/contact?topic=newcomer), `ARTIST_TABS` (Studio→/arts/studio, Products, Orders, Leads, Insights, Coach), `NONPROFIT_TABS` (Workspace→/nonprofit, Survey Says, What2Do, Inbox). `DashTabKey` is now `string` for flexibility.
- Wired into: `components/newcomer-dashboard.tsx` (RESIDENT_TABS, matchedCount), `components/provider-dashboard.tsx` (PROVIDER_TABS; tier/Verified badges moved into rightSlot; action-buttons row kept below), `app/guest-hub/page.tsx` (GUEST_TABS; replaced old primary hero), `components/arts/studio-content.tsx` (ARTIST_TABS; teal banner ABOVE the violet Mona Sierra hero — Mona brand preserved), `components/nonprofit/nonprofit-workspace.tsx` (NONPROFIT_TABS; teal banner ABOVE the rose Sierra Joy hero — preserved).

Two bug fixes shipped same batch:
1. Resident "Want personalized recommendations?" prompt never disappeared — root cause: `app/api/user/profile/route.ts` select omitted `answers`, so `profile.answers.deepOnboarding` was always undefined. Added `answers: true`.
2. Resident "Your Interests" teal chips were dead — made them `<Link href="/directory?tag=<tag>">`. Added `?tag=` filter to `app/api/providers/route.ts` (`where.tags = { has: tag }`) and tag support + clearable indicator chip in `components/directory-content.tsx`.

State: two checkpoints saved ("Teal dashboard topbar + tabs + bug fixes", then "Teal banner + tabs on all role cards"), builds PASS. NOT deployed — Aubre' deploys herself. Phase 2 (role-aware sub-tabs on the Dashboard tab) still deferred pending her go-ahead.

## 2026-08-19 — Phase 2 role switcher + interest-bubble→category fix (built + checkpointed, NOT deployed)

Aubre': "Phase 2 Go- and either remove the industry bubbles OR make them connect to those categories in the directory- whichever". Delivered BOTH, cost-light, no schema changes.

**Phase 2 — RoleSwitcher (`components/dashboard/role-switcher.tsx`, default export):** client component, fetches existing `/api/roles/me`, reads `assignments[]`, renders a "Your workspaces" pill strip linking to each workspace-bearing role the user actually holds, EXCLUDING the current one (prop `current`). Renders NOTHING for a single-role user (a plain resident sees nothing extra). Excludes statuses DENIED/SUSPENDED/EXPIRED. WORKSPACES map (overrides the null dashboardHrefs in lib/roles.ts for ARTIST/PRESS): RESIDENT→/dashboard, BUSINESS→/dashboard/provider, NONPROFIT→/nonprofit, ARTIST→/arts/studio, PRESS→/dashboard/writer, CITY_COUNCIL+CITY_STAFF→/dashboard/municipality (deduped by href), EDC→/opportunity-center. Teal pills (#0F5C5B), ArrowUpRight glyph. Wired in right under the DashboardTopBar in: `components/newcomer-dashboard.tsx` (current="RESIDENT"), `components/provider-dashboard.tsx` (BUSINESS), `components/nonprofit/nonprofit-workspace.tsx` (NONPROFIT), `components/arts/studio-content.tsx` (ARTIST). (guest-hub intentionally left without it.)

**Industry bubbles = the newcomer "Your Interests" chips.** New helper `lib/interest-categories.ts`: `interestHref(tag)` + `interestDestinationLabel(tag)`. TAG_TO_CATEGORY maps onboarding interest tags → REAL directory categories (from components/directory-content.tsx list) so a chip always lands on populated results, e.g. home_repair/landscaping/cleaning/pest_control→'Home Services'; plumbing/electrical/hvac→'AC/Heating/Plumbing/Electrical'; healthcare/dentist→'Healthcare'; pediatrics→'Medical / Pediatric / Nursing Care'; insurance/financial→'Professional Services'; legal→'Legal Services'; real_estate/buying→'Real Estate'; renting→'Apartments'; building→'Architectural Design & Building'; childcare/education/early_childhood/youth_programs/elementary→'Education & Childcare'; youth_soccer/youth_sports→'Youth Sports'; gymnastics/dance/fitness/sports→'Sports & Athletics'; martial_arts→'Martial Arts'; church→'Churches & Worship'; family_dining/restaurants/date_night/coffee→'Dining'; shopping→'Retail & Shopping'; clubs→'Entertainment'; automotive→'Automotive'. Pure-lifestyle tags with no category (social, networking, family, kids, couple, senior, job_seeker, student, etc.) gracefully fall back to `/directory?tag=`. `components/newcomer-dashboard.tsx` interest chips now use `interestHref(tag)` (was raw `?tag=`) + a title tooltip. Rationale: provider CATEGORY is populated on all 454 rows while tags only on 171, so category linking never dead-ends.

Reason interest chips are the "bubbles" (not IndustrySolutionSelector on /solutions & /claim — those are the playbook picker, left untouched). Told Aubre' which set I connected so she can redirect if she meant a different one.

State: build PASS, checkpoint saved. NOT deployed — Aubre' deploys herself.

## 2026-08-19 — Front-door eligibility gate + door sign-in gating (built + checkpointed, NOT deployed)

Aubre's requested flow: land on site → popup "Welcome to Anna TowneLIFE — Do you live or own a business in Anna?" Yes/No. Yes → stay on home doors; No → Guest Pass. Opening a business/nonprofit/artist door → sign-in required. Footer Business Directory → same Yes/No gate first.

- **`lib/welcome-gate.ts`** (new): `GATE_KEY='anna-eligibility:v1'`, `GATE_EVENT='anna:open-gate'`, `openWelcomeGate(target?)` dispatches CustomEvent, `readGateAnswer()` reads localStorage. Answer ('yes'/'no') remembered per browser so returning locals aren't nagged.
- **`components/welcome-gate.tsx`** (new, client): teal (#0F5C5B) modal with radial highlight. Uses `useSession` — never auto-greets authenticated users. Auto-opens after 900ms ONLY on home path `/` for first-time unauthenticated visitors with no stored answer. Also listens for GATE_EVENT to open on demand (with optional target). Yes → store 'yes', navigate to target if provided else stay; No → store 'no', router.push('/welcome/guest'). X close = sessionStorage soft-suppress (`anna-eligibility:dismissed`) for the session only. Mounted in `app/layout.tsx` inside `<Providers><ComfortProvider>` right after `{children}`.
- **`components/gated-link.tsx`** (new, client): renders a `<button>` calling `openWelcomeGate(target)`. Used in footer.
- **`components/footer.tsx`**: FLink type gained optional `gated?: boolean`; DISCOVER 'Business Directory' now `gated: true`; Column renders GatedLink for gated links (footer stays a server component, GatedLink is the only client bit). So footer Business Directory → gate → Yes goes to /directory, No goes to Guest Pass. (Nonprofit/Artist/Sponsors directory links left ungated — can extend if she wants.)
- **`app/page.tsx`**: added `useSession` → `authed`. `GATED_DOORS = {newcomer, neighbor, business, nonprofit, arts, writers}` (NOT guest, NOT magazine). `openDoor` liked-shortcut now routes gated doors to `/login?callbackUrl=<href>` when unauthenticated. Passes `gated` + `authed` to AudienceFilmModal.
- **`components/audience-film-modal.tsx`**: new optional props `gated`/`authed`. When `gated && !authed`, CTA href → `/login?callbackUrl=<ctaHref>`, label → "Sign In to Enter", gateNote → sign-in microcopy. Films preserved otherwise.

State: build PASS, checkpoint saved ("Front-door eligibility gate + door sign-in gating"). NOT deployed — Aubre' deploys herself.

## 2026-08-22 — "Trending in 75409" Google Trends signal on MERCY (built + checkpointed + DEPLOYED both hosts)

Aubre's request: bring Google-Trends-style real-time local search-intent into the MERCY "Start My Day" loop so rising queries become story pitches / merchant Trend Alerts.

- **Data source:** SerpApi's `google_trends` engine (Google has no free official Trends API). Key already existed on her account; copied into `.env` as `SERPAPI_KEY` (value never echoed). Only 200-responses consume a SerpApi search credit; free tier is small & shared across engines.
- **Route `app/api/admin/trends/route.ts`** (GET, force-dynamic, ADMIN-guarded via getServerSession + isAdminEmail): fetches RELATED_QUERIES (rising+top) for seed `q="Anna Texas"`, `geo="US-TX"`, `data_type=RELATED_QUERIES`. **In-memory module cache, 1hr TTL, ONE SerpApi call per refresh** to protect credit budget. All failure modes return HTTP 200 with `success:false` + honest reason (missing key → configured:false; 429 → "monthly credit may be exhausted"; etc.) so widget never shows false trending or crashes. Returns `{success,configured,cached,fetchedAt,seed,geo,rising[],top[]}` where each item = `{query,value,extractedValue}`.
- **Widget `components/mercy/google-trends-widget.tsx`** ('use client'): restyled to MERCY dark petrol/gold theme (NOT her sample's slate/sky). Shows rising queries with Flame icon + gold value badge; falls back to `top` with an honest "no breakout spikes" note if rising empty. Loading/unconfigured/error/empty states all honest. **"Create Story" button is REAL** — POSTs to existing `/api/news/todays-news/create-draft` (creates a StorySuggestion in the newsroom desk queue) with title `Trending in 75409: "<query>"` + angle + Google Trends link; flips to "Filed" on success. Refresh button + "Open Google Trends" link + "Cached/Fresh · refreshes hourly" footer.
- **Mounted** in `app/mercy/page.tsx` after `<TodaysNewsContainer/>`, before the Start My Day button (always visible).
- Verified live: single controlled SerpApi test returned HTTP 200, 9 rising + 25 top, values parsed correctly. Known limitation (told her): Trends related-queries is semantic not strictly geo-fenced, so seed "Anna Texas" can surface non-Anna results (e.g. "santa anna texas", a person named "anna bender"); geo=US-TX narrows it. v1 accepts this honestly.

## 2026-08-22 — Story dispatch (Civic Wire) + "Assign to Writers" on MERCY (built + checkpointed + DEPLOYED both hosts)

Aubre's request: route City of Anna & EDC press releases (and trends) into story assignments across the Writers Desk + Press Pass Network + Sierra Grace newsroom. Her pasted sample used STUBS (console.log, invented `/api/admin/assign-trends`, a non-existent `app/sierra-grace/page.tsx`) + slate/sky styling — I wired it to the app's REAL backbone instead and restyled to MERCY petrol/gold.

- **`app/api/admin/dispatch-story/route.ts`** (POST, force-dynamic, ADMIN-guarded via getServerSession + role/isAdminEmail). Body `{title, sourceUrl?, summary?, type?}`. Fetches every `magazineContributor` where `pressCredential.status==='ACTIVE'` (currently 3 real accounts: Kimberly Christensen, Anna Arts Council, Aubre Murphy). For each: creates a real `WriterAssignment` (writerId, assignedBy=adminId, title, description=summary+source+"Routed from MERCY · <type>", department=type, status 'OPEN') — these surface automatically in the EXISTING writer workspace panel (`components/magazine/writer-workspace.tsx` → `/api/anna-life/assignments`), no new receiver UI needed. Rings each writer's bell via `notify()` (type 'assignment', link '/dashboard/writer', dedupeKey). Files ONE durable `StorySuggestion` (survives zero-writer case). Sequential loop (small N, respects DB conn cap). Returns `{success, assignmentsCreated, notified, deskFiled, writers[], note?}` with honest zero-writer note.
- **`app/api/admin/civic-wire/route.ts`** (GET, force-dynamic, ADMIN-guarded). Google News RSS (free, no key), query `("City of Anna" OR "Anna EDC" OR "Anna Economic Development" OR "Anna City Council" OR "Anna Community Development") Texas when:60d` (started at 21d → returned 0 items; widened to 60d → 4 real relevant items e.g. Rosamond Town Center groundbreaking). Same regex parser as Today's News (split on `<item>` so channel "Google News" title excluded). 1hr in-process cache. Derives `type` per item ('EDC Release' if edc/economic/jobs/groundbreak keywords else 'Municipal Update'). Returns `{success, cached, items:[{title,link,source,pubDate,snippet,type}]}` max 8. Honest error/empty states.
- **`components/mercy/civic-wire-widget.tsx`** ('use client', MERCY dark theme, Landmark icon). Lists civic items; each has "Assign to Writers & Sierra Grace" button → POST dispatch-story; flips to "On N writers' desks" (or "Filed to desk queue" if 0).
- **`components/mercy/google-trends-widget.tsx`**: added header "Assign to Writers" button (amber #F2A93B) → `assignTrends()` composes a digest of top-5 rising/top queries and POSTs to dispatch-story with type 'Trends Lead'; flips to "On N writers' desks".
- **Mounted** CivicWireWidget in `app/mercy/page.tsx` right after GoogleTrendsWidget block.

HONESTY BOUNDARY told to Aubre': NO autonomous Sierra auto-drafting — no endpoint persists an AI-written draft (WriterStory is per-user; Sierra isn't a user). Assignments land on writers' desks where they draft + "Refine with Sierra" (real). Did NOT ship a fake "🤖 Auto-Draft with Sierra" button. StorySuggestion has no admin reader UI yet — it's a durable sink, the visible receiver is WriterAssignment + the bell.

State: build PASS, checkpoint saved, DEPLOYED to annatexas.org + annatexas.abacusai.app (verified live).

## 2026-08-22 — Admin Story Desk Queue reader view (built + checkpointed + DEPLOYED both hosts)

Aubre' asked for a central admin reader so every filed StorySuggestion pitch is visible before a writer claims it.

- **`app/api/admin/story-desk/route.ts`** — GET (force-dynamic, ADMIN-guarded): returns ALL StorySuggestion (take 200, desc), resolves writer names for records with writerId (via magazineContributor lookup), derives a `source` label from the details blob: "Routed from MERCY" → Trends Lead / EDC Release / Civic Wire / MERCY Dispatch; "Filed from Today's News" → Today's News; writerId present → Reader Tip; else Desk Pitch. Returns `{success,total,counts,items[]}`. DELETE `?id=` dismisses a handled pitch (safe cleanup of the desk record only).
- **`components/admin/story-desk.tsx`** — navy/slate admin theme (matches other admin panels: `bg-slate-900/60 border-slate-800 rounded-xl`, NOT MERCY petrol/gold). Source rollup chips at top, per-item cards with source badge + writer target + timeAgo + topic + details + filed-by + mailto + clickable source URL (regex-extracted from details) + Dismiss button. Honest empty/error/loading states.
- **Wired into `components/admin-dashboard.tsx`**: added `'STORY_DESK'` to AdminTab, imported StoryDesk, added nav tab "Story Desk Queue" (ClipboardList amber icon) right after Newsroom, added `case 'STORY_DESK': return <StoryDesk />`.

NOTE: 0 StorySuggestion records in DB at build time (no dispatch fired yet), so the reader currently shows its honest empty state; it populates the moment a Civic Wire/Trends item is dispatched or a Today's News item is filed. Build PASS, checkpoint saved, DEPLOYED to annatexas.org + annatexas.abacusai.app.